{
    "version": 2,
    "framework": "nextjs",
    "installCommand": "npm install",
    "buildCommand": "next build",
    "ignoreCommand": "git log -1 --pretty=%B | grep -q '\\[skip ci\\]'",
    "regions": [
        "sfo1"
    ]
}