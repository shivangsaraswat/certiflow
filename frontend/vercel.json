{
    "version": 2,
    "framework": "nextjs",
    "installCommand": "npm install",
    "buildCommand": "next build",
    "ignoreCommand": "bash -c 'git diff HEAD^ HEAD --quiet -- ../backend && git diff HEAD^ HEAD --quiet -- ../README.md'",
    "regions": [
        "sfo1"
    ]
}